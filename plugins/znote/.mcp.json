{
  "znote-mcp": {
    "command": "uv",
    "args": [
      "--directory",
      "${CLAUDE_PLUGIN_ROOT}/mcp-servers/znote-mcp",
      "run",
      "python",
      "-m",
      "znote_mcp.main"
    ],
    "env": {
      "ZETTELKASTEN_NOTES_DIR": "${HOME}/.zettelkasten/notes",
      "ZETTELKASTEN_DATABASE_PATH": "${HOME}/.zettelkasten/zettelkasten.db"
    }
  }
}
